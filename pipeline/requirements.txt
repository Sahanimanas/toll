opencv-python-headless>=4.9
numpy>=1.26
requests>=2.31
redis>=5.0

# Optional AI backends — install for production accuracy:
# ultralytics>=8.2        # YOLO vehicle + plate detection (GPU-capable)
# easyocr>=1.7            # deep-learning OCR
# pytesseract>=0.3.10     # classical OCR fallback (needs tesseract binary)
